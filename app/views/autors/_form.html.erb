<%= form_with(model: autor, local: true) do |form| %>
  <% if autor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(autor.errors.count, "error") %> prohibited this autor from being saved:</h2>

      <ul>
      <% autor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :autor_name %>
    <%= form.text_field :autor_name, id: :autor_autor_name %>
  </div>

  <div class="field">
    <%= form.label :autor_year %>
    <%= form.number_field :autor_year, id: :autor_autor_year %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
